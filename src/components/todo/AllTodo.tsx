import CreateTodo from "./CreateTodo";
import TodoCards from "./TodoCards";

const AllTodo = () => {
  return (
    <div className="flex">
      {/* <CreateTodo /> */}
      <TodoCards />
    </div>
  );
};

export default AllTodo;
